{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title" : "ApiKey Check Policy",
  "description" : "A policy that only allows requests with API key.",
  "properties": {
    "errorMessage": {
      "type": "string",
      "title": "Error Message",
      "default": "X-API-KEY header was not set."
    }
  }
}